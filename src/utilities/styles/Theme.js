const headingFonts = `'Rajdhani', sans-serif`;
const bodyFonts = `'Open Sans', sans-serif;`;

const fonts = {
    heading: headingFonts,
    body: bodyFonts
};

const colors = {
    black: '#000',
    white: '#fff',
    grey: '#F7F7F7',  
    blue: '#273DFF',
    yellow: '#FBFF3C'
};

const transitions = {
    default: "0.3s ease-in-out"
}

const Theme = {
    colors: colors,
    fonts: fonts,
    transitions: transitions
}


export default Theme;